model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review like an owner.
Prioritize correctness, regressions, stale docs, and missing tests over style-only feedback.
Call out registry mismatches, broken search behavior, and unsupported product claims first.
"""
